%{ 
char longest[1000]="", temp[1000]; 
%} 
 
%% 
[a-zA-Z]+ { 
    if(strlen(yytext) > strlen(longest)) 
        strcpy(longest, yytext); 
} 
.|\n ; 
%% 
 
int main(){ 
    yylex(); 
    printf("Longest word = %s\n", longest); 
} 

Example : car bike metro
OUTPUT:
Longest word = metro
